// import axios from 'axios'

// initial state
const state = () => ({
  actors: []
})

// getters
const getters = {
  actors(state) {
    return state.actors
  }
}

// actions
const actions = {
  
}

// mutations
const mutations = {
  
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}