// import axios from 'axios'

// initial state
const state = () => ({
  tvShows: []
})

// getters
const getters = {
  tvShows(state) {
    return state.tvShows
  }
}

// actions
const actions = {
  
}

// mutations
const mutations = {
  
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}